import React from 'react'
import { SourceFile } from '@components'

const story = {
  title: 'atoms/SourceFile',
  component: SourceFile,
  argTypes: {},
}
export default story

export const Primary = () => (
  
  <SourceFile />

)

